const shopModel = 'shop';

const shopActivityStatuses = {
  active: 'active',
  deactive: 'deactive',
};

const shopPaymentStatuses = {
  paid: 'paid',
  unpaid: 'unpaid',
};

module.exports = {
  shopModel,
  shopActivityStatuses,
  shopPaymentStatuses,
};

const chatModel = 'chat';

const messageModel = 'message';

const chatTypes = {
  buyerSeller: 'buyer-seller',
  sellerAdmin: 'seller-admin',
};

module.exports = {
  chatModel,
  messageModel,
  chatTypes,
};
